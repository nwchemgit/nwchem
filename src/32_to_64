Place holder file to prevent a 32-to-64 bit conversion happening on code
that is "as is in the repository". Make realclean forces a code conversion
phase to happen irrespective of the current state of the code.
